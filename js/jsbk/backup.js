function backup() { alert("Backup er ikke endret i denne fikspakken."); }
function importerBackup() { alert("Import er ikke endret i denne fikspakken."); }
window.backup = backup;
window.importerBackup = importerBackup;
window.backup = backup;
